/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBooleanDecoder,
  getBooleanEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getOptionDecoder,
  getOptionEncoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type Codec,
  type Decoder,
  type Encoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type Option,
  type OptionOrNullable,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { EVENTQUEST_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_CHECKPOINT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([240, 31, 161, 37, 25, 68, 43, 243]);

export function getUpdateCheckpointDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_CHECKPOINT_DISCRIMINATOR,
  );
}

export type UpdateCheckpointInstruction<
  TProgram extends string = typeof EVENTQUEST_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TAccountCheckpoint extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountEvent extends string
        ? ReadonlyAccount<TAccountEvent>
        : TAccountEvent,
      TAccountCheckpoint extends string
        ? WritableAccount<TAccountCheckpoint>
        : TAccountCheckpoint,
      ...TRemainingAccounts,
    ]
  >;

export type UpdateCheckpointInstructionData = {
  discriminator: ReadonlyUint8Array;
  attestor: Option<Address>;
  opensAt: Option<bigint>;
  closesAt: Option<bigint>;
  points: Option<number>;
  active: Option<boolean>;
};

export type UpdateCheckpointInstructionDataArgs = {
  attestor: OptionOrNullable<Address>;
  opensAt: OptionOrNullable<number | bigint>;
  closesAt: OptionOrNullable<number | bigint>;
  points: OptionOrNullable<number>;
  active: OptionOrNullable<boolean>;
};

export function getUpdateCheckpointInstructionDataEncoder(): Encoder<UpdateCheckpointInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["attestor", getOptionEncoder(getAddressEncoder())],
      ["opensAt", getOptionEncoder(getI64Encoder())],
      ["closesAt", getOptionEncoder(getI64Encoder())],
      ["points", getOptionEncoder(getU32Encoder())],
      ["active", getOptionEncoder(getBooleanEncoder())],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_CHECKPOINT_DISCRIMINATOR }),
  );
}

export function getUpdateCheckpointInstructionDataDecoder(): Decoder<UpdateCheckpointInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["attestor", getOptionDecoder(getAddressDecoder())],
    ["opensAt", getOptionDecoder(getI64Decoder())],
    ["closesAt", getOptionDecoder(getI64Decoder())],
    ["points", getOptionDecoder(getU32Decoder())],
    ["active", getOptionDecoder(getBooleanDecoder())],
  ]);
}

export function getUpdateCheckpointInstructionDataCodec(): Codec<
  UpdateCheckpointInstructionDataArgs,
  UpdateCheckpointInstructionData
> {
  return combineCodec(
    getUpdateCheckpointInstructionDataEncoder(),
    getUpdateCheckpointInstructionDataDecoder(),
  );
}

export type UpdateCheckpointInput<
  TAccountAuthority extends string = string,
  TAccountEvent extends string = string,
  TAccountCheckpoint extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  event: Address<TAccountEvent>;
  checkpoint: Address<TAccountCheckpoint>;
  attestor: UpdateCheckpointInstructionDataArgs["attestor"];
  opensAt: UpdateCheckpointInstructionDataArgs["opensAt"];
  closesAt: UpdateCheckpointInstructionDataArgs["closesAt"];
  points: UpdateCheckpointInstructionDataArgs["points"];
  active: UpdateCheckpointInstructionDataArgs["active"];
};

export function getUpdateCheckpointInstruction<
  TAccountAuthority extends string,
  TAccountEvent extends string,
  TAccountCheckpoint extends string,
  TProgramAddress extends Address = typeof EVENTQUEST_PROGRAM_ADDRESS,
>(
  input: UpdateCheckpointInput<
    TAccountAuthority,
    TAccountEvent,
    TAccountCheckpoint
  >,
  config?: { programAddress?: TProgramAddress },
): UpdateCheckpointInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountEvent,
  TAccountCheckpoint
> {
  // Program address.
  const programAddress = config?.programAddress ?? EVENTQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: false },
    checkpoint: { value: input.checkpoint ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("event", accounts.event),
      getAccountMeta("checkpoint", accounts.checkpoint),
    ],
    data: getUpdateCheckpointInstructionDataEncoder().encode(
      args as UpdateCheckpointInstructionDataArgs,
    ),
    programAddress,
  } as UpdateCheckpointInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountEvent,
    TAccountCheckpoint
  >);
}

export type ParsedUpdateCheckpointInstruction<
  TProgram extends string = typeof EVENTQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    event: TAccountMetas[1];
    checkpoint: TAccountMetas[2];
  };
  data: UpdateCheckpointInstructionData;
};

export function parseUpdateCheckpointInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdateCheckpointInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      event: getNextAccount(),
      checkpoint: getNextAccount(),
    },
    data: getUpdateCheckpointInstructionDataDecoder().decode(instruction.data),
  };
}
