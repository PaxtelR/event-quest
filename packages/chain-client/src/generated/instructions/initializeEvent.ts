/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findEventPda } from "../pdas";
import { EVENTQUEST_PROGRAM_ADDRESS } from "../programs";

export const INITIALIZE_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([126, 249, 86, 221, 202, 171, 134, 20]);

export function getInitializeEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    INITIALIZE_EVENT_DISCRIMINATOR,
  );
}

export type InitializeEventInstruction<
  TProgram extends string = typeof EVENTQUEST_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountEvent extends string
        ? WritableAccount<TAccountEvent>
        : TAccountEvent,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type InitializeEventInstructionData = {
  discriminator: ReadonlyUint8Array;
  externalIdHash: ReadonlyUint8Array;
  startsAt: bigint;
  endsAt: bigint;
};

export type InitializeEventInstructionDataArgs = {
  externalIdHash: ReadonlyUint8Array;
  startsAt: number | bigint;
  endsAt: number | bigint;
};

export function getInitializeEventInstructionDataEncoder(): FixedSizeEncoder<InitializeEventInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["externalIdHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["startsAt", getI64Encoder()],
      ["endsAt", getI64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: INITIALIZE_EVENT_DISCRIMINATOR }),
  );
}

export function getInitializeEventInstructionDataDecoder(): FixedSizeDecoder<InitializeEventInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["externalIdHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["startsAt", getI64Decoder()],
    ["endsAt", getI64Decoder()],
  ]);
}

export function getInitializeEventInstructionDataCodec(): FixedSizeCodec<
  InitializeEventInstructionDataArgs,
  InitializeEventInstructionData
> {
  return combineCodec(
    getInitializeEventInstructionDataEncoder(),
    getInitializeEventInstructionDataDecoder(),
  );
}

export type InitializeEventAsyncInput<
  TAccountAuthority extends string = string,
  TAccountEvent extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  event?: Address<TAccountEvent>;
  systemProgram?: Address<TAccountSystemProgram>;
  externalIdHash: InitializeEventInstructionDataArgs["externalIdHash"];
  startsAt: InitializeEventInstructionDataArgs["startsAt"];
  endsAt: InitializeEventInstructionDataArgs["endsAt"];
};

export async function getInitializeEventInstructionAsync<
  TAccountAuthority extends string,
  TAccountEvent extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof EVENTQUEST_PROGRAM_ADDRESS,
>(
  input: InitializeEventAsyncInput<
    TAccountAuthority,
    TAccountEvent,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  InitializeEventInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountEvent,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? EVENTQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    event: { value: input.event ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.event.value) {
    accounts.event.value = await findEventPda({
      authority: getAddressFromResolvedInstructionAccount(
        "authority",
        accounts.authority.value,
      ),
      externalIdHash: getNonNullResolvedInstructionInput(
        "externalIdHash",
        args.externalIdHash,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("event", accounts.event),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeEventInstructionDataEncoder().encode(
      args as InitializeEventInstructionDataArgs,
    ),
    programAddress,
  } as InitializeEventInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountEvent,
    TAccountSystemProgram
  >);
}

export type InitializeEventInput<
  TAccountAuthority extends string = string,
  TAccountEvent extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  event: Address<TAccountEvent>;
  systemProgram?: Address<TAccountSystemProgram>;
  externalIdHash: InitializeEventInstructionDataArgs["externalIdHash"];
  startsAt: InitializeEventInstructionDataArgs["startsAt"];
  endsAt: InitializeEventInstructionDataArgs["endsAt"];
};

export function getInitializeEventInstruction<
  TAccountAuthority extends string,
  TAccountEvent extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof EVENTQUEST_PROGRAM_ADDRESS,
>(
  input: InitializeEventInput<
    TAccountAuthority,
    TAccountEvent,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): InitializeEventInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountEvent,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? EVENTQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    event: { value: input.event ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("event", accounts.event),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getInitializeEventInstructionDataEncoder().encode(
      args as InitializeEventInstructionDataArgs,
    ),
    programAddress,
  } as InitializeEventInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountEvent,
    TAccountSystemProgram
  >);
}

export type ParsedInitializeEventInstruction<
  TProgram extends string = typeof EVENTQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    event: TAccountMetas[1];
    systemProgram: TAccountMetas[2];
  };
  data: InitializeEventInstructionData;
};

export function parseInitializeEventInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedInitializeEventInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      event: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getInitializeEventInstructionDataDecoder().decode(instruction.data),
  };
}
