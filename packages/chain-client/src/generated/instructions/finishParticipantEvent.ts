/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { EVENTQUEST_PROGRAM_ADDRESS } from "../programs";

export const FINISH_PARTICIPANT_EVENT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([248, 101, 31, 11, 204, 208, 29, 91]);

export function getFinishParticipantEventDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    FINISH_PARTICIPANT_EVENT_DISCRIMINATOR,
  );
}

export type FinishParticipantEventInstruction<
  TProgram extends string = typeof EVENTQUEST_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TAccountParticipantEvent extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? ReadonlySignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountEvent extends string
        ? ReadonlyAccount<TAccountEvent>
        : TAccountEvent,
      TAccountParticipantEvent extends string
        ? WritableAccount<TAccountParticipantEvent>
        : TAccountParticipantEvent,
      ...TRemainingAccounts,
    ]
  >;

export type FinishParticipantEventInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type FinishParticipantEventInstructionDataArgs = {};

export function getFinishParticipantEventInstructionDataEncoder(): FixedSizeEncoder<FinishParticipantEventInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: FINISH_PARTICIPANT_EVENT_DISCRIMINATOR,
    }),
  );
}

export function getFinishParticipantEventInstructionDataDecoder(): FixedSizeDecoder<FinishParticipantEventInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getFinishParticipantEventInstructionDataCodec(): FixedSizeCodec<
  FinishParticipantEventInstructionDataArgs,
  FinishParticipantEventInstructionData
> {
  return combineCodec(
    getFinishParticipantEventInstructionDataEncoder(),
    getFinishParticipantEventInstructionDataDecoder(),
  );
}

export type FinishParticipantEventInput<
  TAccountAuthority extends string = string,
  TAccountEvent extends string = string,
  TAccountParticipantEvent extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  event: Address<TAccountEvent>;
  participantEvent: Address<TAccountParticipantEvent>;
};

export function getFinishParticipantEventInstruction<
  TAccountAuthority extends string,
  TAccountEvent extends string,
  TAccountParticipantEvent extends string,
  TProgramAddress extends Address = typeof EVENTQUEST_PROGRAM_ADDRESS,
>(
  input: FinishParticipantEventInput<
    TAccountAuthority,
    TAccountEvent,
    TAccountParticipantEvent
  >,
  config?: { programAddress?: TProgramAddress },
): FinishParticipantEventInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountEvent,
  TAccountParticipantEvent
> {
  // Program address.
  const programAddress = config?.programAddress ?? EVENTQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: false },
    participantEvent: {
      value: input.participantEvent ?? null,
      isWritable: true,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("event", accounts.event),
      getAccountMeta("participantEvent", accounts.participantEvent),
    ],
    data: getFinishParticipantEventInstructionDataEncoder().encode({}),
    programAddress,
  } as FinishParticipantEventInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountEvent,
    TAccountParticipantEvent
  >);
}

export type ParsedFinishParticipantEventInstruction<
  TProgram extends string = typeof EVENTQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    event: TAccountMetas[1];
    participantEvent: TAccountMetas[2];
  };
  data: FinishParticipantEventInstructionData;
};

export function parseFinishParticipantEventInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedFinishParticipantEventInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      event: getNextAccount(),
      participantEvent: getNextAccount(),
    },
    data: getFinishParticipantEventInstructionDataDecoder().decode(
      instruction.data,
    ),
  };
}
