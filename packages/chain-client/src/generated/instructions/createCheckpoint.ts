/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressDecoder,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getI64Decoder,
  getI64Encoder,
  getStructDecoder,
  getStructEncoder,
  getU32Decoder,
  getU32Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  getNonNullResolvedInstructionInput,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findCheckpointPda } from "../pdas";
import { EVENTQUEST_PROGRAM_ADDRESS } from "../programs";

export const CREATE_CHECKPOINT_DISCRIMINATOR: ReadonlyUint8Array =
  new Uint8Array([87, 58, 2, 214, 112, 193, 234, 5]);

export function getCreateCheckpointDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CREATE_CHECKPOINT_DISCRIMINATOR,
  );
}

export type CreateCheckpointInstruction<
  TProgram extends string = typeof EVENTQUEST_PROGRAM_ADDRESS,
  TAccountAuthority extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TAccountCheckpoint extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountAuthority extends string
        ? WritableSignerAccount<TAccountAuthority> &
            AccountSignerMeta<TAccountAuthority>
        : TAccountAuthority,
      TAccountEvent extends string
        ? WritableAccount<TAccountEvent>
        : TAccountEvent,
      TAccountCheckpoint extends string
        ? WritableAccount<TAccountCheckpoint>
        : TAccountCheckpoint,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CreateCheckpointInstructionData = {
  discriminator: ReadonlyUint8Array;
  externalIdHash: ReadonlyUint8Array;
  attestor: Address;
  opensAt: bigint;
  closesAt: bigint;
  points: number;
};

export type CreateCheckpointInstructionDataArgs = {
  externalIdHash: ReadonlyUint8Array;
  attestor: Address;
  opensAt: number | bigint;
  closesAt: number | bigint;
  points: number;
};

export function getCreateCheckpointInstructionDataEncoder(): FixedSizeEncoder<CreateCheckpointInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["externalIdHash", fixEncoderSize(getBytesEncoder(), 32)],
      ["attestor", getAddressEncoder()],
      ["opensAt", getI64Encoder()],
      ["closesAt", getI64Encoder()],
      ["points", getU32Encoder()],
    ]),
    (value) => ({ ...value, discriminator: CREATE_CHECKPOINT_DISCRIMINATOR }),
  );
}

export function getCreateCheckpointInstructionDataDecoder(): FixedSizeDecoder<CreateCheckpointInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["externalIdHash", fixDecoderSize(getBytesDecoder(), 32)],
    ["attestor", getAddressDecoder()],
    ["opensAt", getI64Decoder()],
    ["closesAt", getI64Decoder()],
    ["points", getU32Decoder()],
  ]);
}

export function getCreateCheckpointInstructionDataCodec(): FixedSizeCodec<
  CreateCheckpointInstructionDataArgs,
  CreateCheckpointInstructionData
> {
  return combineCodec(
    getCreateCheckpointInstructionDataEncoder(),
    getCreateCheckpointInstructionDataDecoder(),
  );
}

export type CreateCheckpointAsyncInput<
  TAccountAuthority extends string = string,
  TAccountEvent extends string = string,
  TAccountCheckpoint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  event: Address<TAccountEvent>;
  checkpoint?: Address<TAccountCheckpoint>;
  systemProgram?: Address<TAccountSystemProgram>;
  externalIdHash: CreateCheckpointInstructionDataArgs["externalIdHash"];
  attestor: CreateCheckpointInstructionDataArgs["attestor"];
  opensAt: CreateCheckpointInstructionDataArgs["opensAt"];
  closesAt: CreateCheckpointInstructionDataArgs["closesAt"];
  points: CreateCheckpointInstructionDataArgs["points"];
};

export async function getCreateCheckpointInstructionAsync<
  TAccountAuthority extends string,
  TAccountEvent extends string,
  TAccountCheckpoint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof EVENTQUEST_PROGRAM_ADDRESS,
>(
  input: CreateCheckpointAsyncInput<
    TAccountAuthority,
    TAccountEvent,
    TAccountCheckpoint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CreateCheckpointInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountEvent,
    TAccountCheckpoint,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? EVENTQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    event: { value: input.event ?? null, isWritable: true },
    checkpoint: { value: input.checkpoint ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.checkpoint.value) {
    accounts.checkpoint.value = await findCheckpointPda({
      event: getAddressFromResolvedInstructionAccount(
        "event",
        accounts.event.value,
      ),
      externalIdHash: getNonNullResolvedInstructionInput(
        "externalIdHash",
        args.externalIdHash,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("event", accounts.event),
      getAccountMeta("checkpoint", accounts.checkpoint),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateCheckpointInstructionDataEncoder().encode(
      args as CreateCheckpointInstructionDataArgs,
    ),
    programAddress,
  } as CreateCheckpointInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountEvent,
    TAccountCheckpoint,
    TAccountSystemProgram
  >);
}

export type CreateCheckpointInput<
  TAccountAuthority extends string = string,
  TAccountEvent extends string = string,
  TAccountCheckpoint extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  authority: TransactionSigner<TAccountAuthority>;
  event: Address<TAccountEvent>;
  checkpoint: Address<TAccountCheckpoint>;
  systemProgram?: Address<TAccountSystemProgram>;
  externalIdHash: CreateCheckpointInstructionDataArgs["externalIdHash"];
  attestor: CreateCheckpointInstructionDataArgs["attestor"];
  opensAt: CreateCheckpointInstructionDataArgs["opensAt"];
  closesAt: CreateCheckpointInstructionDataArgs["closesAt"];
  points: CreateCheckpointInstructionDataArgs["points"];
};

export function getCreateCheckpointInstruction<
  TAccountAuthority extends string,
  TAccountEvent extends string,
  TAccountCheckpoint extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof EVENTQUEST_PROGRAM_ADDRESS,
>(
  input: CreateCheckpointInput<
    TAccountAuthority,
    TAccountEvent,
    TAccountCheckpoint,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CreateCheckpointInstruction<
  TProgramAddress,
  TAccountAuthority,
  TAccountEvent,
  TAccountCheckpoint,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? EVENTQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    authority: { value: input.authority ?? null, isWritable: true },
    event: { value: input.event ?? null, isWritable: true },
    checkpoint: { value: input.checkpoint ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("authority", accounts.authority),
      getAccountMeta("event", accounts.event),
      getAccountMeta("checkpoint", accounts.checkpoint),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCreateCheckpointInstructionDataEncoder().encode(
      args as CreateCheckpointInstructionDataArgs,
    ),
    programAddress,
  } as CreateCheckpointInstruction<
    TProgramAddress,
    TAccountAuthority,
    TAccountEvent,
    TAccountCheckpoint,
    TAccountSystemProgram
  >);
}

export type ParsedCreateCheckpointInstruction<
  TProgram extends string = typeof EVENTQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    authority: TAccountMetas[0];
    event: TAccountMetas[1];
    checkpoint: TAccountMetas[2];
    systemProgram: TAccountMetas[3];
  };
  data: CreateCheckpointInstructionData;
};

export function parseCreateCheckpointInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCreateCheckpointInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 4) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 4,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      authority: getNextAccount(),
      event: getNextAccount(),
      checkpoint: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCreateCheckpointInstructionDataDecoder().decode(instruction.data),
  };
}
