/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findAttendancePda, findParticipantEventPda } from "../pdas";
import { EVENTQUEST_PROGRAM_ADDRESS } from "../programs";

export const CHECK_IN_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  209, 253, 4, 217, 250, 241, 207, 50,
]);

export function getCheckInDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CHECK_IN_DISCRIMINATOR);
}

export type CheckInInstruction<
  TProgram extends string = typeof EVENTQUEST_PROGRAM_ADDRESS,
  TAccountParticipant extends string | AccountMeta<string> = string,
  TAccountAttestor extends string | AccountMeta<string> = string,
  TAccountEvent extends string | AccountMeta<string> = string,
  TAccountCheckpoint extends string | AccountMeta<string> = string,
  TAccountParticipantEvent extends string | AccountMeta<string> = string,
  TAccountAttendance extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountParticipant extends string
        ? WritableSignerAccount<TAccountParticipant> &
            AccountSignerMeta<TAccountParticipant>
        : TAccountParticipant,
      TAccountAttestor extends string
        ? ReadonlySignerAccount<TAccountAttestor> &
            AccountSignerMeta<TAccountAttestor>
        : TAccountAttestor,
      TAccountEvent extends string
        ? WritableAccount<TAccountEvent>
        : TAccountEvent,
      TAccountCheckpoint extends string
        ? WritableAccount<TAccountCheckpoint>
        : TAccountCheckpoint,
      TAccountParticipantEvent extends string
        ? WritableAccount<TAccountParticipantEvent>
        : TAccountParticipantEvent,
      TAccountAttendance extends string
        ? WritableAccount<TAccountAttendance>
        : TAccountAttendance,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CheckInInstructionData = {
  discriminator: ReadonlyUint8Array;
  challengeHash: ReadonlyUint8Array;
};

export type CheckInInstructionDataArgs = { challengeHash: ReadonlyUint8Array };

export function getCheckInInstructionDataEncoder(): FixedSizeEncoder<CheckInInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["challengeHash", fixEncoderSize(getBytesEncoder(), 32)],
    ]),
    (value) => ({ ...value, discriminator: CHECK_IN_DISCRIMINATOR }),
  );
}

export function getCheckInInstructionDataDecoder(): FixedSizeDecoder<CheckInInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["challengeHash", fixDecoderSize(getBytesDecoder(), 32)],
  ]);
}

export function getCheckInInstructionDataCodec(): FixedSizeCodec<
  CheckInInstructionDataArgs,
  CheckInInstructionData
> {
  return combineCodec(
    getCheckInInstructionDataEncoder(),
    getCheckInInstructionDataDecoder(),
  );
}

export type CheckInAsyncInput<
  TAccountParticipant extends string = string,
  TAccountAttestor extends string = string,
  TAccountEvent extends string = string,
  TAccountCheckpoint extends string = string,
  TAccountParticipantEvent extends string = string,
  TAccountAttendance extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  participant: TransactionSigner<TAccountParticipant>;
  /**
   * Backend attestor for this checkpoint. Must match `checkpoint.attestor`
   * — the program never trusts a JWT or Redis state, only this signature.
   */
  attestor: TransactionSigner<TAccountAttestor>;
  event: Address<TAccountEvent>;
  checkpoint: Address<TAccountCheckpoint>;
  participantEvent?: Address<TAccountParticipantEvent>;
  attendance?: Address<TAccountAttendance>;
  systemProgram?: Address<TAccountSystemProgram>;
  challengeHash: CheckInInstructionDataArgs["challengeHash"];
};

export async function getCheckInInstructionAsync<
  TAccountParticipant extends string,
  TAccountAttestor extends string,
  TAccountEvent extends string,
  TAccountCheckpoint extends string,
  TAccountParticipantEvent extends string,
  TAccountAttendance extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof EVENTQUEST_PROGRAM_ADDRESS,
>(
  input: CheckInAsyncInput<
    TAccountParticipant,
    TAccountAttestor,
    TAccountEvent,
    TAccountCheckpoint,
    TAccountParticipantEvent,
    TAccountAttendance,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CheckInInstruction<
    TProgramAddress,
    TAccountParticipant,
    TAccountAttestor,
    TAccountEvent,
    TAccountCheckpoint,
    TAccountParticipantEvent,
    TAccountAttendance,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? EVENTQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    participant: { value: input.participant ?? null, isWritable: true },
    attestor: { value: input.attestor ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: true },
    checkpoint: { value: input.checkpoint ?? null, isWritable: true },
    participantEvent: {
      value: input.participantEvent ?? null,
      isWritable: true,
    },
    attendance: { value: input.attendance ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.participantEvent.value) {
    accounts.participantEvent.value = await findParticipantEventPda({
      event: getAddressFromResolvedInstructionAccount(
        "event",
        accounts.event.value,
      ),
      participant: getAddressFromResolvedInstructionAccount(
        "participant",
        accounts.participant.value,
      ),
    });
  }
  if (!accounts.attendance.value) {
    accounts.attendance.value = await findAttendancePda({
      event: getAddressFromResolvedInstructionAccount(
        "event",
        accounts.event.value,
      ),
      checkpoint: getAddressFromResolvedInstructionAccount(
        "checkpoint",
        accounts.checkpoint.value,
      ),
      participant: getAddressFromResolvedInstructionAccount(
        "participant",
        accounts.participant.value,
      ),
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("participant", accounts.participant),
      getAccountMeta("attestor", accounts.attestor),
      getAccountMeta("event", accounts.event),
      getAccountMeta("checkpoint", accounts.checkpoint),
      getAccountMeta("participantEvent", accounts.participantEvent),
      getAccountMeta("attendance", accounts.attendance),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCheckInInstructionDataEncoder().encode(
      args as CheckInInstructionDataArgs,
    ),
    programAddress,
  } as CheckInInstruction<
    TProgramAddress,
    TAccountParticipant,
    TAccountAttestor,
    TAccountEvent,
    TAccountCheckpoint,
    TAccountParticipantEvent,
    TAccountAttendance,
    TAccountSystemProgram
  >);
}

export type CheckInInput<
  TAccountParticipant extends string = string,
  TAccountAttestor extends string = string,
  TAccountEvent extends string = string,
  TAccountCheckpoint extends string = string,
  TAccountParticipantEvent extends string = string,
  TAccountAttendance extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  participant: TransactionSigner<TAccountParticipant>;
  /**
   * Backend attestor for this checkpoint. Must match `checkpoint.attestor`
   * — the program never trusts a JWT or Redis state, only this signature.
   */
  attestor: TransactionSigner<TAccountAttestor>;
  event: Address<TAccountEvent>;
  checkpoint: Address<TAccountCheckpoint>;
  participantEvent: Address<TAccountParticipantEvent>;
  attendance: Address<TAccountAttendance>;
  systemProgram?: Address<TAccountSystemProgram>;
  challengeHash: CheckInInstructionDataArgs["challengeHash"];
};

export function getCheckInInstruction<
  TAccountParticipant extends string,
  TAccountAttestor extends string,
  TAccountEvent extends string,
  TAccountCheckpoint extends string,
  TAccountParticipantEvent extends string,
  TAccountAttendance extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof EVENTQUEST_PROGRAM_ADDRESS,
>(
  input: CheckInInput<
    TAccountParticipant,
    TAccountAttestor,
    TAccountEvent,
    TAccountCheckpoint,
    TAccountParticipantEvent,
    TAccountAttendance,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CheckInInstruction<
  TProgramAddress,
  TAccountParticipant,
  TAccountAttestor,
  TAccountEvent,
  TAccountCheckpoint,
  TAccountParticipantEvent,
  TAccountAttendance,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? EVENTQUEST_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    participant: { value: input.participant ?? null, isWritable: true },
    attestor: { value: input.attestor ?? null, isWritable: false },
    event: { value: input.event ?? null, isWritable: true },
    checkpoint: { value: input.checkpoint ?? null, isWritable: true },
    participantEvent: {
      value: input.participantEvent ?? null,
      isWritable: true,
    },
    attendance: { value: input.attendance ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("participant", accounts.participant),
      getAccountMeta("attestor", accounts.attestor),
      getAccountMeta("event", accounts.event),
      getAccountMeta("checkpoint", accounts.checkpoint),
      getAccountMeta("participantEvent", accounts.participantEvent),
      getAccountMeta("attendance", accounts.attendance),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getCheckInInstructionDataEncoder().encode(
      args as CheckInInstructionDataArgs,
    ),
    programAddress,
  } as CheckInInstruction<
    TProgramAddress,
    TAccountParticipant,
    TAccountAttestor,
    TAccountEvent,
    TAccountCheckpoint,
    TAccountParticipantEvent,
    TAccountAttendance,
    TAccountSystemProgram
  >);
}

export type ParsedCheckInInstruction<
  TProgram extends string = typeof EVENTQUEST_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    participant: TAccountMetas[0];
    /**
     * Backend attestor for this checkpoint. Must match `checkpoint.attestor`
     * — the program never trusts a JWT or Redis state, only this signature.
     */
    attestor: TAccountMetas[1];
    event: TAccountMetas[2];
    checkpoint: TAccountMetas[3];
    participantEvent: TAccountMetas[4];
    attendance: TAccountMetas[5];
    systemProgram: TAccountMetas[6];
  };
  data: CheckInInstructionData;
};

export function parseCheckInInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCheckInInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 7) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 7,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      participant: getNextAccount(),
      attestor: getNextAccount(),
      event: getNextAccount(),
      checkpoint: getNextAccount(),
      participantEvent: getNextAccount(),
      attendance: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCheckInInstructionDataDecoder().decode(instruction.data),
  };
}
