//! Shared environment/config loading for `apps/api` and `apps/indexer`,
//! including the startup gate that refuses to boot unless
//! `SOLANA_NETWORK=devnet` (spec §12.1). Placeholder — implemented in
//! Phase 3.
