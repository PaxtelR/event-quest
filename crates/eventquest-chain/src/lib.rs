//! Shared Solana client code (account layouts, instruction builders, event
//! decoding) generated/derived from the `eventquest` program IDL, used by
//! both `apps/api` and `apps/indexer` so account layouts aren't duplicated —
//! see docs/adr/ADR-002-solana-client.md. Placeholder — implemented in
//! Phase 2.
